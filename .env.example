# Generate a secure .env automatically:  bash scripts/init-env.sh
# Or fill in each value manually below.

# Database
POSTGRES_PASSWORD=

# Object storage
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=

# Redis
REDIS_PASSWORD=

# Authentication
JWT_SECRET=

# Fernet encryption key (encrypts user API keys at rest)
# Generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# LLM (default provider; other providers configured per user in the app)
DEEPSEEK_API_KEY=
